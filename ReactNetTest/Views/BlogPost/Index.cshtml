@using Newtonsoft.Json
@model ReactNetTest.Models.BlogPost.BlogPost
    
@Html.React("BlogPost", Model, containerId: "blogpost")

<script id="blogpostdata">
    @Html.Raw(JsonConvert.SerializeObject(Model))
</script>